function calTotalPointOfBasketballGame(twoPointCount, threePointCount) {
  // #1
  // let result = twoPointCount * 2 + threePointCount * 3;
  // return result;

  // #2
  return twoPointCount * 2 + threePointCount * 3;
}
